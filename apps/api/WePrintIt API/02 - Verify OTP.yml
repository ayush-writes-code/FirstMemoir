info:
  name: 02 - Verify OTP
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/v1/auth/verify-otp"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​