info:
  name: Send OTP
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/v1/auth/send-otp"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
