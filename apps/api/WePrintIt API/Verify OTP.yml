info:
  name: Verify OTP
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3001/api/v1/auth/verify-otp
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
