info:
  name: Verify Authentication
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3001/api/v1/auth/me
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
