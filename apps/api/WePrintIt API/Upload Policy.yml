info:
  name: Upload Policy
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3001/api/v1/storage/upload-policy
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
