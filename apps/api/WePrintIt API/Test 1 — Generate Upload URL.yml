info:
  name: Test 1 — Generate Upload URL
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3001/api/v1/storage/upload-url
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
