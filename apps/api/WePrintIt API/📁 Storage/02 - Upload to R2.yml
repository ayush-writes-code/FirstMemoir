info:
  name: 02 - Upload to R2
  type: http

http:
  method: GET
  url: https://.r2.cloudflarestorage.com/
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
