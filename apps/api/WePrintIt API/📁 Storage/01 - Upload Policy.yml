info:
  name: 01 - Upload Policy
  type: http

http:
  method: POST
  url: "{{baseUrl}}/api/v1/storage/upload-policy"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
